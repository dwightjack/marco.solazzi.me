import React from 'react';
import pure from '../../base/pure';
import Title from '../Title';

export const MainTitle = () => (
    <Title className="c-main-title" prefix="me" title="jobs.current" />
);

export default pure(MainTitle);
