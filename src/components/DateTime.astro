---
import { toIso, toMonthYear } from '../shared/dateTime';

export interface Props {
  datetime: string | Date;
}

const { datetime } = Astro.props;
---

<time datetime={toIso(datetime)}>{toMonthYear(datetime)}</time>
